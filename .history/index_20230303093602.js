// function display()
let grid = []];
function createArray(rows, columns) {
        var arr = new Array(rows);
        for (var i = 0; i < rows; i++) arr[i] = new Array(columns);
    

createGrid();
console.log(grid);
function placeItem(column) {}
